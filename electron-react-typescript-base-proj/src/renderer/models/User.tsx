export type User = {
    userId: string; // 유저 ID
    lastConvoId: string; // 마지막으로 연 대화방 ID
    locale: string; // 유저 locale
    createdAt: string; // 유저 생성 일시
    updatedAt: string; // 유저 수정 일시
  };
  
//   export type IState = Message[];