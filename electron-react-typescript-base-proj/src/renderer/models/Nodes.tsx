export type Nodes = {
    columnText: string,
    email?: string,
    hasChildren: boolean
    parentCode: string
    photo?:string,
    showChildren: boolean
    subTree:[]
    type: string,
    value: string,
}