export enum MSG {
    USER = 'user',
    SYSTIME = 'systemTime',
    SYSETR = 'systemEnter'
}

export enum RoomType {
    BOT = 'bot',
    DOC = 'doc',
    DEP = 'dep'
}

export enum SearchType{
    ROOM = 'room',
    USER = 'user'
}

export enum HeaderType{
    CHAT = 'chatRoom',
    CREATE = 'createChatRoom',
    ETC = 'etc',
}


