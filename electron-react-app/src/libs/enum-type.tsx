export enum MSG {
    USER = 'user',
    SYSTEM = 'system'
}

export enum RoomType {
    BOT = 'bot',
    DOC = 'doc',
    DEP = 'dep'
}

export enum HeaderType{
    CHAT = 'chatRoom',
    CREATE = 'createChatRoom',
    ETC = 'etc',
}
