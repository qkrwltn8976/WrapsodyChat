import React, { Component, Fragment } from 'react';
import { Client } from "@stomp/stompjs";

class BotChatRoom extends Component {
    
    client;
    name;
    state = 
    client = new Client()'
    this.client.brokerURL = "ws://localhost:8080";
    this
    componentDidMount() {
        <script src = "stomp.js">
            let ws = new WebSocket('ws://127.0.0.1:15674/ws');
            var client = Stomp.over(ws);

        </script>
    }
    render() {
        return (
            <div>
                <h3>채팅방리스트</h3>
            </div>
        )
    }
}

export default BotChatRoom;